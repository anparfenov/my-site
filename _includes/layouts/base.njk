<!doctype html>
<html lang="ru">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ title or metadata.title }}</title>
        <meta name="description" content="{{ description or metadata.description }}">
        <link rel="stylesheet" href="{{ '/css/' + css['index.css'] | url }}">
        <link rel="stylesheet" href="{{ '/css/prism-vs.css' | url }}">

        <link rel="preconnect" href="https://fonts.gstatic.com">
        <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono&family=Raleway:wght@400;800;900&display=swap" rel="stylesheet">

        <link rel="alternate" href="{{ metadata.feed.path | url }}" type="application/atom+xml" title="{{ metadata.title }}">
        <link rel="alternate" href="{{ metadata.jsonfeed.path | url }}" type="application/json" title="{{ metadata.title }}">

        <!-- https://bugzilla.mozilla.org/show_bug.cgi?id=1404468 -->
        <script>/**/</script>
    </head>
    <body class="s-common s-light-theme default-layout">
        {% include "components/header.njk" %}

        <main class="{% if templateClass %}{{ templateClass }}{% endif %}">
            {{ content | safe }}
        </main>

        {% include "components/footer.njk" %}
    </body>
</html>
